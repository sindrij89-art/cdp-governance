# ==============================================
# CDP Governance - Environment Variables
# ==============================================
# Copy this file to .env.local and fill in values.
# Do NOT commit .env.local to version control.

# --- Supabase ---
# Create a 'pilot_applications' table in Supabase (see SQL in deliverables).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# If neither variable is set, the API returns HTTP 501 (NOT_CONFIGURED).
